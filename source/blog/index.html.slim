---
title: Defman's Blog
description: Defman's blog - articles about different things
type: website
name: Defman's Blog
js:
    - search
---
= partial "partials/search", locals: {file: "articles"}
- sorted = current_page.children.sort_by { |page| page.data.date } .reverse!
- sorted[0...5].each do |page|
    - y, m, d, h, min = page.data.date.to_s.match(/(\d{4})(\d{2})(\d{2})(\d{2})(\d{2})/i).captures
    .container.artcicle id=page.data.url
        span.title = link_to page.data.title, page.url
        span.timestamp
            i.fa.fa-clock-o>
            = "#{y}/#{m}/#{d} #{h}:#{min}"
        span.tags
            i.fa.fa-tags>
            = page.data.tags
        .content.description
            = truncate_with_ellipses page.data.description, 150
- if sorted.length > 5
    .pagination
        = link_to "=>", '/blog/pages/2'
